manifestVersion: subsquid.io/v0.1
name: farther
version: 1
description: 'Farther-related Uniswap data'
build: 
  package_manager: pnpm
deploy:
  secrets:
    - RPC_ENDPOINT
  addons:
    rpc:
      - base.http
    postgres: 
  processor:
    cmd:
      - sqd
      - process:prod
  api:
    cmd:
      - sqd
      - serve:prod
